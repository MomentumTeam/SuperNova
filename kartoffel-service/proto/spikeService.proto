syntax = "proto3";

package Spike;

service Spike {
    rpc GetSpikeToken(GetSpikeTokenRequest) returns (SpikeToken) {}
}

message GetSpikeTokenRequest{
    // string audienceId = 1;
}

message SpikeToken {
    string token = 1;
}